# ccsliinc is working on

updated: 2026-09-10T20:30Z

- `release/1.2.1` - assembled at `546443e`, NOT tagged and NOT deployed.
  Carries adoom666's plan, mute, theme restore and row menu, plus our
  listing socket-scope fix, the index-backed row read, the permission
  re-look throttle, and the one superset row menu the owner ruled. An
  agent is closing a test gap and a file split in that worktree now.
- `feat/svelte-1.3` - the Svelte 5 migration and the typed plugin surface
  registry. **Now published to `adamdev` and `origin` at `d6801cb`.** It
  was on neither remote until today, which is why `web/` was invisible to
  adoom666.
- `feat/coordination-protocol` - this protocol, plus an evaluation of the
  `.claude/skills/coord/` skill adoom666 offered.

Paused, not abandoned: slices 2 to 7 of the launchpad Svelte migration.

If you are adoom666, read `notes/ccsliinc-to-adoom666-row-menu-merged.md`
first. It answers the revert question (do not revert, we merged it),
corrects one assumption in your row menu claim, and reports a finding in
your restart runner.

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# Data Validation
# UPPER BOUND IS DELIBERATE, AND IT IS INSURANCE, NOT A MIGRATION.
# src/models.py still carries 10 pydantic-2 deprecated sites: 5 `class
# Config` blocks and 5 `json_encoders`. Inside 2.x they emit warnings and
# keep working. Pydantic 3.0 removes both, so an UNBOUNDED requirement
# would let the next fresh install resolve to 3.x and turn all 10 into
# hard ImportErrors at startup, with no warning release in between - a
# break that arrives on a day nobody here changed anything.
# The pin buys time; it does not pay the debt. The real fix is
# ConfigDict plus field serializers, which is its own job. Do not raise
# this ceiling until that migration has landed and been measured.
pydantic>=2.10.0,<3
pydantic-settings>=2.6.0

# Authentication
pyotp>=2.9.0
qrcode>=7.4.2
pillow>=10.0.0
pyjwt>=2.8.0

# Rate Limiting / Replay Defense
slowapi>=0.1.9
cachetools>=5.3.0

# Async SQLite (refresh-token revocation store)
aiosqlite>=0.20.0

# Logging
structlog==23.2.0

# HTTP Client (for health checks)
httpx==0.25.2

# Cloudflare API
cloudflare>=2.11.0,<3.0
pyyaml>=6.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

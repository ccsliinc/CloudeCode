# Cloude Code — Active TODO

Orchestrator owns structure. Sub-agents append findings to "Sub-Agent Findings" section using format `[AGENT-NAME] [TIMESTAMP]: finding`.

---

## Active Initiatives

### 1. Pluggable Theme System + Agent-Type Labeling
**Spec:** `/Users/Adam/.claude/plans/read-the-readme-md-and-groovy-locket.md`
**Status:** Approved spec, captures done, fingerprints authored, ready for implementation
**Open dependencies:** None (CLI banner captures collected on 2026-04-24)

**Phases (in order):**
- [ ] Phase 1 — CSS variable migration in `client/css/styles.css` (~30 hardcoded colors → semantic vars in `:root`, including dpad)
- [ ] Phase 2 — Theme registry + selector (`client/js/themes/registry.js`, `themeSelector.js`, header `<select>`, localStorage persistence, manifest validator that skip-on-fail)
- [ ] Phase 3 — 5 bundled themes (`client/css/themes/{claude,codex,hermes,claw,matrix}/theme.json` + optional theme.css for bundled, + Matrix effects.js)
- [ ] Phase 4 — Per-screen scope wiring (global theme on `<html>`, session theme on `#terminal-screen[data-session-theme=X]`, gated on `replay_in_progress`)
- [ ] Phase 5 — xterm.js theme swap + WebGL onContextLoss handler (verify empirically; only add `term.refresh()` if visual stale-paint observed)
- [ ] Phase 6 — Agent-type backend (`ProjectConfig.agent_type`, `AgentsConfig`, models extended, lifespan backfill, command resolution in `create_session`)
- [ ] Phase 7 — `agent_fingerprint.py` with REAL patterns (claude/codex/hermes/openclaw — see "Fingerprint Patterns" section below)
- [ ] Phase 8 — Launchpad agent badges + `client/assets/agents/*.svg` copy + agentIcons.js
- [ ] Phase 9 — `/api/v1/themes` endpoint + user themes mount at Application Support dir + NoCacheStaticFiles `.json` extension fix + JS-consent prompt
- [ ] Phase 10 — Tests (`test_agent_fingerprint`, `test_themes_endpoint`, `test_session_agent_type`)
- [ ] Phase 11 — validator-agent UI verification per Phase (see spec Verification section, 16 numbered checks)
- [ ] Phase 12 — git commit per `CLAUDE.md` GITCOMMIT rules (security-review first)

**Success criteria:** all 16 numbered verification checks in spec pass, no regression on Claude theme, Matrix rain throttles correctly on mobile.

---

### 2. BUG: HTTP 500 on destroying external session "insidersApp"
**Status:** In progress — `BUG-FIX` sub-agent dispatched 2026-04-24
**Symptom:** Clicking X on the "insidersApp" session row returns `HTTP 500 - destroy failed`. Session has been RUNNING + EXTERNAL for 21h.
**Acceptance:** insidersApp gone from launchpad, validator-agent confirms X works on a fresh external session.

---

## Fingerprint Patterns (drafted from real captures 2026-04-24)

Captures saved at `/Users/Adam/Dropbox/llmScratch/{claude,codex,hermes,openclaw}.txt`. Match order at runtime: openclaw → hermes → codex → claude (most specific first). Each agent has 3 patterns OR'd.

```python
import re
AGENT_FINGERPRINTS = {
    "claude": [
        "Claude Code'll be able to read, edit, and execute files",
        re.compile(r"^\s*❯\s*1\.\s*Yes, I trust this folder", re.M),
        "Security guide",
    ],
    "codex": [
        ">_ OpenAI Codex (v",
        re.compile(r"^\s*gpt-[\d.]+\s+(default\s+)?fast\s+·\s+~", re.M),
        "/model to change",
    ],
    "hermes": [
        re.compile(r"Hermes Agent v[\d.]+"),
        "Welcome to Hermes Agent! Type your message or /help",
        re.compile(r"⚕\s+\S+\s+│\s+ctx\s+\S+\s+│\s+\[[░▒▓█\s]*\]"),
    ],
    "openclaw": [
        "🦞 OpenClaw",
        re.compile(r"^\s*openclaw tui - ws://[\d.]+:\d+ - agent \S+ - session \S+", re.M),
        "I'm not magic—I'm just extremely persistent with retries",
    ],
}
```

Search strategy: `tmux capture-pane -p -t <session> -S -2000` first attempt, fall back to steady-state status-bar patterns on re-checks.

CLI invocations (corrected per user 2026-04-24):
| agent_type | command |
|------------|---------|
| claude     | `claude --dangerously-skip-permissions` |
| codex      | `codex` |
| hermes     | `hermes` |
| openclaw   | `openclaw tui` |

---

## Sub-Agent Findings

(Sub-agents append below this line. Format: `[AGENT-NAME] [TIMESTAMP]: finding`. Never overwrite existing.)
[ITEM-2] [2026-04-19T12:16:34Z]: completed — TunnelManager router + LocalOnlyBackend/QuickCloudflareBackend/NamedCloudflareBackend created under src/core/tunnel/. HybridTunnelManager preserved as legacy shim. config.py + config.example.json + .env.example updated. main.py wires TunnelManager.from_settings and guards verify_public_access on backend.supports_public(). auto_tunnel.py updated to work with dict-shape tunnels. 34/34 tests pass. Cloudflare-less boot confirmed via /health smoke.
[ITEM-3] [2026-04-19T12:24:00Z]: completed — WS auth moved from `?token=` query string to `Sec-WebSocket-Protocol` subprotocol. Added `verify_jwt_from_subprotocol` in `src/api/deps.py` (marker = `cloude.jwt.v1`). `src/api/websocket.py` now validates pre-accept, closes 4401 on auth failure / 4400 on malformed header, and calls `accept(subprotocol="cloude.jwt.v1")` to echo the marker (required by RFC 6455 or browsers drop the connection). Client: `api.js` adds `openWebSocket()` using the two-element `['cloude.jwt.v1', token]` subprotocol array; `terminal.js` now uses it. 17 new tests (10 unit + 7 integration via TestClient) all pass; 51/51 total. Live WS smoke against running server confirms valid token connects and echoes marker, while no-subprotocol / bad-token / legacy `?token=` all get rejected with HTTP 403. **BREAKING**: pre-existing clients still using `?token=<jwt>` must switch to the subprotocol form.
[ITEM-4] [2026-04-19T12:30:00Z]: completed — TOTP brute-force + replay defense. Added `slowapi>=0.1.9` + `cachetools>=5.3.0` to requirements. New `AuthRateLimits` dataclass in `src/config.py` with `totp_verify_per_minute` (5), `totp_verify_per_hour` (20), `trust_proxy_headers` (false) — loader parses optional `auth_rate_limits` JSON block with same malformed-block tolerance as session/tunnel. `src/api/auth.py` now exports a module-level `Limiter(key_func=_rate_limit_key, headers_enabled=True)`; `_rate_limit_key` honors XFF only when `trust_proxy_headers=True`. `verify_totp` gained `request: Request, response: Response` params, `@limiter.limit(_totp_rate_limit)` decorator (config-driven `"N/minute;M/hour"` string), and a `TTLCache(maxsize=1000, ttl=90)` guarded by `asyncio.Lock` for code-reuse dedup — replay returns `401 {"detail":{"success":false,"reason":"code_reused"}}`. `src/main.py` wires `app.state.limiter`, `RateLimitExceeded` handler, and `SlowAPIMiddleware` after CORS. 5 new tests (total 56/56 pass). Smoke: 7 rapid POSTs → 5x401, then 429+429 with `Retry-After: 60` on every response. No duplicate log spam on 429 (correction #9).
[ITEM-6] [2026-04-19T12:51:00Z]: completed — Notification pipeline (ntfy.sh) wired end-to-end. NEW: `src/core/notifications/{__init__,events,ntfy,router}.py`. `events.py` defines `EventType` enum (PERMISSION_PROMPT/INPUT_REQUIRED/TASK_COMPLETE/ERROR/BUILD_COMPLETE/TEST_RESULT/TUNNEL_CREATED), `NotificationEvent` dataclass (kind/session_slug/timestamp/snippet — slug + snippet INTERNAL ONLY, never sent to ntfy), and `build_deep_link()` helper (URL-encoded slug, returns None when public_base_url empty). `ntfy.py` is a plain module (NO ABC — YAGNI per Plan v3.1 CUT 6) with module-level `_client` httpx singleton, `init/shutdown/rotate_topic/send` async helpers, and an `_EVENT_PRESENTATION` table mapping each EventType to generic Title (no project name, no slug), short body, ntfy Priority (1-5: PERMISSION=5, INPUT=4, others=3), comma-sep emoji-friendly Tags, and Click-header URL composition. All errors (TimeoutException, RequestError, 4xx/5xx) caught + logged WARN, never raised. `router.py` provides `NotificationRouter` with bounded `asyncio.Queue(maxsize=100)`, drop-OLDEST overflow policy (pop one + retry put + log both events), SYNCHRONOUS non-blocking `emit()` safe from PTY callbacks, async worker drains queue and dispatches via `ntfy.send()`. EDITED: `src/config.py` adds `NotificationsConfig(enabled=False, ntfy_base_url='https://ntfy.sh', ntfy_topic='', public_base_url='')` to `AuthConfig`, loader parses optional `notifications` block with same malformed-tolerance pattern as session/tunnel. `config.example.json` adds the `notifications` block. `src/main.py` lifespan creates `NotificationRouter` AFTER log_monitor and BEFORE auto_tunnel/tunnel_manager, awaits `ntfy.init(base_url, topic)` then `router.start()`, stores on `app.state.notification_router`; shutdown awaits `router.stop()` then `ntfy.shutdown()`. `setup_auth.py`: NEW interactive `setup_notifications_block(config_path)` (asks y/N, generates 32-hex topic via `secrets.token_hex(16)` prefixed with `cloude-`, prompts for public_base_url with mDNS hostname default, HEAD-probes `/health` with 3s timeout — warn-and-continue), and NEW `--rotate-topic` argparse subcommand that mutates `config.json` notifications.ntfy_topic in place and prints the new full subscription URL. Removed shadowing `import sys` inside `main()`. Tests: 17 NEW in `tests/test_notifications.py` (build_deep_link basic/trailing-slash/url-encoding/None, ntfy.send URL+Title+Priority+Tags+Click verification, privacy assertion that NO ntfy field contains test slug, swallows 500/timeout/ConnectError, skipped on empty topic, omits Click without public_base_url, rotate_topic swap, router drop-oldest on overflow, router worker drains 5 events via queue.join(), enabled=False noop, topic-missing noop, start/stop clean). FULL SUITE: 85/85 pass. Smoke: import-from-shell OK; live ntfy.sh dispatch OK (no exceptions, generic title); `setup_auth.py --rotate-topic` OK (writes new topic, prints subscription URL).
[ITEM-5] [2026-04-19T12:45:00Z]: completed — JWT access + refresh token pair with SQLite-backed rotation + reuse detection + chain revocation. Deps: `aiosqlite>=0.20.0`. NEW: `src/core/refresh_store.py` (RefreshStore class — aiosqlite + asyncio.Lock, schema `jti TEXT PK, user, exp, revoked, superseded_by, superseded_at` + idx on exp, methods `init/issue/rotate/revoke/revoke_chain/is_valid/is_superseded/purge_expired`). EDITED: `src/api/auth.py` (added `create_access_token`, `create_refresh_token` via `secrets.token_urlsafe(32)` jti, `decode_access_token`, `decode_refresh_token`, private `_decode_with_typ` with explicit `algorithms=["HS256"]` guard + `typ` enforcement; legacy `verify_jwt_token` kept as shim; `/auth/verify` now mints BOTH tokens and persists refresh jti; NEW `/auth/refresh` endpoint (rate-limited 30/min) with rotate-and-retry reuse detection + chain-revoke; NEW `/auth/logout` endpoint with best-effort revoke). EDITED: `src/api/deps.py` (WS subprotocol path uses `decode_access_token` so `typ=="access"` enforced; added `get_refresh_store` dep). EDITED: `src/models.py` (`AuthTokenResponse` extended with `access_token` / `refresh_token` / `expires_in`, deprecated `token` alias kept for one-release compat). EDITED: `src/main.py` (lifespan installs `RefreshStore` at `$LOG_DIRECTORY/refresh_tokens.db`, starts `_refresh_purge_loop` on 6h cadence, cancels + closes on shutdown). EDITED: `src/config.py` (`AuthConfig` gains `access_token_ttl_seconds=900`, `refresh_token_ttl_seconds=604800`, `refresh_grace_seconds=10`; loader accepts optional JSON overrides). EDITED: `client/js/auth.js` (stores `claude_refresh_token` in localStorage, adds `refresh()` / `getRefreshToken` / `setRefreshToken`, `logout()` best-effort server-side revoke). EDITED: `client/js/api.js` (401 interceptor with single-flight `_refreshPromise` mutex — prevents chain-burn under concurrent 401s — transparently replays the original request post-refresh). Tests: 12 new in `tests/test_refresh_tokens.py` covering mint-pair / access-typ / refresh-typ + jti / rotation-with-supersede / within-grace is_valid / post-grace chain-burn / revoked-refresh-rejected / refresh-typ-rejected-on-protected / access-typ-accepted / logout-revokes / purge-expired / access-token-at-refresh-endpoint-rejected. 68/68 all green. Smoke: live server on :8765 verified — TOTP mint returns both tokens + `token` alias==access_token; refresh endpoint rotates; access token accepted on `/auth/status` (200) while refresh token rejected (401); within-grace replay of old refresh returns polite 401 without chain burn; NEW refresh still works after grace-path replay; SQLite DB materialized at `$LOG_DIRECTORY/refresh_tokens.db` with exact spec schema + exp index.
[ITEM-8] [2026-04-19T13:10:00Z]: completed — Notification rate limiter (Plan v3.1 CUT 3 YAGNI). NEW: `src/core/notifications/rate_limit.py` — `RateLimiter` class (38 executable LOC) with global token bucket (`deque[float]`, default 10/60s rolling window) + per-kind cooldown via `_last_by_kind: dict[EventType, float]` (default 10s). `time.monotonic()` everywhere. NOT thread-safe by design (single async worker consumes queue, no locking needed). `check(event)` returns `(allowed, reason)`: per-kind cooldown evaluated first (dedup beats global), then time-based prune of the rolling window, then cap check; on pass, state updated; on suppression, state untouched. `seed_cold_start()` primes every `EventType.last_by_kind = now` to swallow startup storms if scrollback replay bypasses the replay guard (defense in depth). EDITED: `src/core/notifications/router.py` — import `RateLimiter`, instantiate in `__init__` with config getattr fallbacks, call `seed_cold_start()` at tail of `start()`, gate `_run()` worker loop with `allowed, reason = rate_limiter.check(event)` → log `notify.suppressed kind=<kind> session_slug=<slug> reason=<reason>` at INFO via existing structlog logger, `task_done()` + `continue` to keep queue accounting correct. EDITED: `src/config.py` `NotificationsConfig` gains `rate_limit_global_cap: int = 10 (ge=1)`, `rate_limit_window_seconds: float = 60.0 (ge=1.0)`, `rate_limit_per_kind_cooldown_seconds: float = 10.0 (ge=0.0)`. EDITED: `config.example.json` notifications block gets the three new knobs. EDITED: `tests/test_notifications.py` `_FakeNotificationsConfig` extended with permissive rate-limit fields (`global_cap=10_000, per_kind_cooldown=0.0`) so pre-existing tests still pass uncapped. Tests: 7 NEW in `tests/test_rate_limiter.py` — per-kind cooldown suppresses 9/10 same-kind burst; different kinds iterate and hit `global_cap` at event 11; window expiry clears bucket; `per_kind_cooldown_s=0` → only global gate; cold-start seeding blocks immediate emit for every EventType then passes after cooldown elapses; router integration (15-event TASK_COMPLETE burst → ntfy.send called < 15); router integration cold-start (first emit after `start()` suppressed). FULL SUITE: 118/118 pass. Smoke: `./venv/bin/python -c "from src.core.notifications.rate_limit import RateLimiter; print('OK')"` → OK.
[ITEM-9] [2026-04-19T13:20:00Z]: completed — Deep-link routing + CSP middleware. NEW: `client/js/router.js` (IIFE `window.Router` with strict `SLUG_RX = /^[A-Za-z0-9_\- ]+$/`, `DEEPLINK_RX = /^\/session\/([^\/]+)\/?$/`, visible-reject-not-silent-strip error banner, popstate listener, delivers stashed target via `Launchpad.openProjectByName()` after `authenticated` event or immediately if already authed). NEW: `tests/test_deep_link_routing.py` (15 tests: build_deep_link basic + trailing-slash + defensive-slugify + None-base + encoding; _slugify regression for `/` → `_`, `..evil` → `evil`, `safe..name` → `safe__name`, empty → `default`; FastAPI TestClient covers `/session/<name>` 200 + doctype, `/session/valid_project-1.2.3` accepted at route level, CSP headers on `/` + `/session/foo` + `/health`, no `X-Powered-By` or `Set-Cookie` leakage). EDITED: `src/main.py` adds `@app.middleware("http") csp_headers` AFTER CORS (response-path outermost) stamping `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self' ws: wss:; img-src 'self' data:; font-src 'self' data:; frame-ancestors 'none'` + `X-Content-Type-Options: nosniff` + `Referrer-Policy: no-referrer` on every response (including 405 error paths); adds `@app.get("/session/{project}")` below root `/` returning the same `index.html` — specific route before any catch-all, no `/static`/`/ws`/`/api`/`/health` collision. EDITED: `src/core/notifications/events.py` `build_deep_link` now defense-in-depth re-runs `_slugify()` before `quote(safe="")` so a hostile in-process caller constructing `NotificationEvent` directly can never ship a raw `.`, `/`, `?`, or `#` to the push channel. EDITED: `client/index.html` adds `#deep-link-error` red banner div at top-of-body + `<script src="/static/js/router.js">` after all app modules + `window.Router.init()` call after `App.init()` on `load`. EDITED: `client/js/launchpad.js` adds `openProjectByName(name)` — 2s wait for projects to load, exact-match then case-insensitive fallback, `showError` on miss, delegates to existing `selectProject` on hit. EDITED: `setup_auth.py` httpx preflight now passes `verify=False` for self-signed LAN certs (liveness check only, not auth check) + new tap-to-validate hint block printed after notifications write, instructing the user to open the `public_base_url` on their phone to confirm end-to-end LAN reachability. EDITED: `tests/test_notifications.py` existing `test_build_deep_link_url_encodes_slug` updated to match new defensive-slugify contract (path safety assertions unchanged — no `?`/`#`/`/`/space in path segment; exact value now `weird_slug_with_chars_frag`). No `?next=` usage anywhere in the codebase — open-redirect guard N/A. Full suite: 133/133 pass (was 118, +15 new). Live smoke on :8765 confirms CSP header present on `/`, `/session/myproject`, `/health`; `/session/myproject` body starts with `<!DOCTYPE html>`; headers also land on 405 error responses (proof middleware wraps everything).
[ITEM-7] [2026-04-19T13:00:00Z]: completed — IdleWatcher FSM for PTY output → notification events. NEW: `src/core/notifications/idle_watcher.py` (module-level compiled regexes `PROMPT_TOP_RX` / `PROMPT_BOTTOM_RX` with line-anchored ╭─╮/╰─╯ matching requiring BOTH corners, `PERMISSION_RX` line-anchored `[1-9]. Yes|No|Allow|Approve|Deny` to kill grep-output false positives, `TOOL_RUNNING_RX`, `INTERRUPT_RX` matching `^C`, `ANSI_STRIP_RX` covering SGR + OSC + charset-switch escapes; `IdleState` enum with `THINKING` / `TOOL_RUNNING` / `WAITING_PERMISSION` / `IDLE` / `INTERRUPTED`; `IdleWatcher` class — 16KB ring tail_buffer, `time.monotonic()` everywhere, `asyncio.Lock` around all state mutations, `start()` spawns 1s-cadence `_poll_loop`, `stop()` idempotent, `handle_chunk` classifies per-chunk for INTERRUPT (chunk-local) + tail-wide for PERMISSION/TOOL/default, fires `PERMISSION_PROMPT` synchronously on state EDGE (no double-fire), `_poll_once` fires `TASK_COMPLETE` only when state ∈ {THINKING} AND both prompt-frame corners present AND ≥ threshold_s silence, then transitions to IDLE for dedup; fire-and-forget `_emit` with 200-char truncated snippet; `replay_in_progress` flag suspends emits and `last_output_ts` updates but still feeds tail buffer so post-replay state reads correct). EDITED: `src/core/notifications/__init__.py` exports `IdleWatcher` / `IdleState`. EDITED: `src/config.py` `NotificationsConfig` gains `idle_threshold_seconds: float = 30.0` (ge=1.0). EDITED: `config.example.json` adds `idle_threshold_seconds: 30.0` to notifications block. EDITED: `src/core/session_manager.py` imports `IdleWatcher`, adds `self.idle_watcher`/`self._notification_router`, `attach_notification_router(router)` setter called from lifespan, `create_session` instantiates watcher + awaits `.start()` (threshold from config), both failure-path and `destroy_session` stop watcher BEFORE backend to avoid last-gasp races. EDITED: `src/api/websocket.py` scrollback-replay block flips `idle_watcher.replay_in_progress` AND feeds the replayed bytes through `handle_chunk` so the tail warms up without firing events; live send_pty_output hot path calls `session_manager.idle_watcher.handle_chunk(raw_bytes)` after `log_monitor._detect_patterns`, guarded by in_replay + try/except so notification errors never break terminal streaming. EDITED: `src/main.py` lifespan calls `session_manager.attach_notification_router(notification_router)` right after router.start(). Tests: 26 new in `tests/test_idle_watcher.py` — regex positive/negative (6), adversarial corpus (grep Allow / markdown blockquote / bash prompt / single-corner all non-emitting), happy path (TASK_COMPLETE + PERMISSION_PROMPT with monkeypatched monotonic clock), state machine (INTERRUPT exits on non-interrupt chunk, INTERRUPT suppresses TASK_COMPLETE, TOOL_RUNNING suspends idle even past threshold, TASK_COMPLETE dedups via IDLE, permission doesn't double-fire on same state), replay safety (suppresses permission / doesn't touch last_output_ts / live emission works after replay), race safety (asyncio.gather 4 feeders + 4 pollers × 50 iterations each → consistent state + no lost tail trims), ring buffer trim-from-left semantics, start/stop idempotence, snippet truncation to 200. FULL SUITE: 111/111 pass (was 85). Adversarial-corpus coverage: grep `Allow` / markdown `>` / bash prompt / single top-corner / Ctrl-C at threshold all confirmed NON-emitting. Threshold: 30.0s default, tunable via config.
[CSP-FIX] [2026-04-19T13:30:00Z]: CDN allowlisted in CSP for xterm.js. Follow-up: self-host xterm assets in a later pass to tighten CSP back to 'self' only.
[PHASE-2-REGISTRY] [2026-04-24]: completed — `ThemeManifest` model added to `src/models.py`; `GET /api/v1/themes` route added to `src/api/routes.py` (auth-gated, skip-on-fail validator covering JSON-parse / pydantic / id-vs-dirname mismatch / duplicate-id, scans `client/css/themes/*` + `~/Library/Application Support/cloude-code-menubar/themes/*` with `CLOUDE_USER_THEMES_DIR` env override). Bundled Claude manifest written to `client/css/themes/claude/theme.json` (id=claude, 69 cssVars sourced verbatim from `:root` block, 19-key xterm palette copied from `terminal.js` lines 124-144) — validates against pydantic model. Client registry at `client/js/themes/registry.js` exposing `window.Themes` (init/applyGlobal/applySession-stub/clearSession/getActiveGlobal/listAll/onXtermThemeChange + `applyStoredThemeIdSync` for FOUC-kill). Selector at `client/js/themes/themeSelector.js` exposing `window.ThemeSelector.mount(parent)` — inserts `<select id="theme-selector" aria-label="Theme">` BEFORE `#destroySessionBtn` in `.header .controls`. `index.html` wired with `<link id="theme-css">` placeholder and the two new `<script>` tags before `app.js`. `app.js` paints stored theme synchronously on init, then calls `_initThemes()` post-auth (covers both the verifyToken-success path AND the live `authenticated` event). `styles.css` got a minimal `.theme-selector` block (theme-aware, inline-SVG caret in currentColor, hover/focus states, options inherit elevated bg). Server restart clean (no errors), unauth GET → 401 (expected), discovery scan returns 1 builtin (claude). All JS node --check OK.
[ITEM-13] [2026-04-19T14:00:00Z]: completed — .dockerignore written at repo root. Excludes VCS, Python bytecode/venvs, node_modules, IDE, macOS Electron artifacts (macOS/, dist/), .env* + secrets (totp-qr.png, refresh_tokens.db, session_metadata.json, logs/, projects/, .claude/), and all *.md. Whitelist exceptions for .env.example + README.md placed AFTER their broader excludes per dockerignore last-match-wins semantics.
[ITEM-10] [2026-04-19T13:40:00Z]: completed — Dockerfile at `/Users/Adam/Dropbox/My Projects/Cloude Code Repos/Dev/cloudecode/Dockerfile`. Single-stage `python:3.12-slim` (bookworm). System deps: tmux/git/ripgrep/jq/curl/ca-certificates/libsecret-1-0/build-essential with apt-list cleanup. Node.js 22 via NodeSource. `npm install -g @anthropic-ai/claude-code` unpinned per MVP plan. Python deps layer-cached (requirements.txt COPYed before src/). COPY src/, client/, config.example.json, setup_auth.py, setup.sh into /app. Non-root user `cloude` with `ARG UID=1000 / ARG GID=1000` for compose-time host-UID parity. `chown -R cloude:cloude /app` + `USER cloude`. ENV TERM=xterm-256color (correction #10 for docker exec -it), PYTHONUNBUFFERED=1, PYTHONDONTWRITEBYTECODE=1, CLOUDECODE_BIND_HOST=0.0.0.0. EXPOSE 8000. HEALTHCHECK curl /health (30s/5s/3/20s). CMD `["python3","-m","src.main"]`. **Smoke: SKIPPED — Docker daemon not running (`Cannot connect to the Docker daemon at unix:///var/run/docker.sock`). Build/run verification deferred to when daemon is up; per spec, no retry/install attempted.** Image size: unmeasured (daemon down).
[ITEM-14] [2026-04-19T14:20:00Z]: completed — README.md Deployment Modes section added between Installation and Configuration (Mode 1 macOS native primary, Mode 2 Docker alternative with explicit Pro/Max OAuth + macOS-native MCP "NOT supported" callouts, hybrid mode explicitly labeled "cut from this release"). NEW `docs/deployment-docker.md` (~150 lines, 10 sections: prerequisites, first-time setup, preflight rationale, build+run, volume layout table, auth options table, MCP compatibility matrix with ✅/❌, networking, updating, troubleshooting table). Smoke: README grep Mode 1/Mode 2/Mode 3/Mode 4 counts match expectations, deployment-docker.md line count in 150-300 range.
[PHASE-3-THEMES] [2026-04-24]: completed — Shipped 4 bundled themes (codex, hermes, claw, matrix) under `client/css/themes/<id>/`. Each `theme.json` validates: id matches dirname, 64 cssVars override (covers all 11 foundational vars from spec + accent ramp + status + badges + danger/warning/info ramps + on-accent + syntax + shadow-modal — only the 5 truly-shared tokens left to inherit from `:root`: `--font-mono`, `--radius-sm/md/lg/pill/full`), 19-key xterm palette per theme. Codex: light indigo (`#7c5cff` on `#f7f7fa`), gradient body wash + softened light-mode shadows in `theme.css` (34 lines). Hermes: amber CRT (`#ffaa00` on black) with header text-glow + faint scanline overlay (51 lines). Claw: cosmic dark (`#e6e6ea` on `#0a0810`) with multi-layer radial-gradient starfield rasterized once (zero-CPU) (57 lines). Matrix: phosphor green (`#00ff41` on black), all 4 badge colors tinted phosphor per DAR, `text-shadow: 0 0 2px currentColor` + glowing input bottom-border + 65%-opacity card scrim so falling code never harms readability + `prefers-reduced-motion` static-glow fallback (91 lines). Matrix `effects.js` (140 lines, vanilla canvas, ES module, `init`/`destroy` exports + default export): mounts fixed full-viewport canvas at `z-index:-1`, RAF loop draws katakana + digits + latin glyphs falling in `Math.floor(innerWidth/fontSize)` columns, throttled via timestamp-delta to ~30fps desktop / ~15fps mobile (<768px), DPR clamped to 2, occasional bright head-glyph (`#ccffcc`, p=0.015) for sparkle, `prefers-reduced-motion: reduce` short-circuits `init()` and never mounts canvas (CSS fallback handles static look), `document.visibilitychange` pauses/resumes RAF, `resize` recomputes `colCount` + font-size, `destroy()` cancels RAF + removes both event listeners + removes canvas + nulls all module state (idempotent). Verification: all 4 manifests parse via `python3 json.load`, foundational-var coverage = 100%, no theme invents vars outside Claude's 69-var set EXCEPT `--color-header-bg` which the prompt's foundational list requires (Claude manifest is missing it — flagged below). WCAG AA contrast (math): codex 16.28:1 body / 4.06:1 accent (PASS AA, AA-large), hermes 11.00:1 / 13.89:1, claw 15.99:1 / 6.54:1, matrix 15.38:1 / 15.38:1 — all clear. Muted fg/bg: codex 5.37:1, hermes 7.09:1, claw 8.43:1, matrix 6.79:1 (all PASS AA body). `node --check effects.js` clean. **FLAG for next pass:** the `:root` block in `client/css/styles.css` and the bundled Claude manifest are both missing `--color-header-bg` (the spec's foundational list names it, all 4 new themes set it, but the existing claude manifest doesn't and there's no `:root` default). Without a Phase-1 amendment to add `--color-header-bg: var(--color-bg)` (or similar) to `:root`, the var only resolves when one of the 4 new themes is active — claude theme will fall back to whatever inherits from parent which today is fine because `.header` is hardcoded against other vars. Theme.css files for all 4 themes use the var via the `#terminal-screen[data-session-theme="<id>"]` and `:root[data-theme="<id>"] .header` rules where applicable, so behavior is correct under each new theme; only Claude is exposed if/when `.header` ever gets refactored to use `var(--color-header-bg)`. Files (lines): codex/theme.json (88), codex/theme.css (34), hermes/theme.json (87), hermes/theme.css (51), claw/theme.json (87), claw/theme.css (57), matrix/theme.json (88), matrix/theme.css (91), matrix/effects.js (140). Suggested validator-agent check post-Phase-4-wiring: load each theme via the selector, screenshot launchpad (matrix should show falling katakana behind cards with cards still readable), enter+leave a session under each theme to confirm terminal-scope CSS hooks fire, toggle `prefers-reduced-motion: reduce` in DevTools and reload Matrix to confirm canvas does not mount.
[ITEM-11] [2026-04-19T13:50:00Z]: completed — NEW `docker-compose.yml` (v3.9) at repo root + NEW `scripts/preflight-bind-ip.sh` (executable) + `.env.example` appended with Docker (Mode 2) section (CLOUDE_BIND_IP / CLOUDE_PROJECT_PATH / CLOUDE_LOG_DIR + UID/GID build-args hint). Compose: single `cloude` service, image `cloude-code:local`, build args UID/GID from host env (default 1000), port publish binds to `${CLOUDE_BIND_IP:-127.0.0.1}:8000:8000` (secure default — loopback-only if unset, never leaks to 0.0.0.0). Bind mounts: `${HOME}/.claude` → `/home/cloude/.claude` (credentials + MCP), `${CLOUDE_PROJECT_PATH:-./projects}` → `/workspace`, `./config.json` → `/app/config.json:ro`, `./.env` → `/app/.env:ro`, `${CLOUDE_LOG_DIR:-./logs}` → `/var/log/cloude`. Environment: `CLOUDECODE_LOG_DIR=/var/log/cloude`, `CLOUDECODE_DEFAULT_WORKING_DIR=/workspace`, `TZ=${TZ:-UTC}`. `restart: unless-stopped`. Healthcheck mirrors Dockerfile: `curl -fsS http://127.0.0.1:8000/health` with 30s/5s/3/20s. No Cloudflare service (Mode 2 = LAN-only per plan; Mode 1 Electron or manual cloudflared sidecar for public). No named volumes — bind mounts only. Preflight script: opt-in manual run, validates `CLOUDE_BIND_IP` against `ifconfig` output. Cases: unset → warn + list live non-loopback/non-APIPA IPs + exit 0; loopback/wildcard → ✓ + exit 0; IP not on any iface → ✗ + list current IPs + remediation + exit 1; IP live → ✓ + exit 0. Smoke: (1) `docker compose config` — passed, resolved full tree with `host_ip: 127.0.0.1`. (2) YAML parse — v3.9, service `cloude`. (3) Preflight unset — exit 0, listed `en13 -> 192.168.1.250`. (4) Preflight `10.99.99.99` — exit 1 with ✗ + current IPs. (5) Preflight `127.0.0.1` — exit 0 with ✓. `.env.example` Cloudflare section (lines 27-41) untouched.
[PHASE-6-BACKEND-AGENT] [2026-04-24]: completed — Backend agent-type plumbing live. **config.py**: `ProjectConfig` gains `agent_type: Literal["claude","codex","hermes","openclaw"] = "claude"` (per-project default, backward-compat — missing field defaults to claude). NEW `AgentsConfig` model with the corrected commands as defaults (`claude --dangerously-skip-permissions`, `codex`, `hermes`, `openclaw tui`) — stored as shell-strings to match the existing `f"{claude_cli} --dangerously-skip-permissions"` shape that `TmuxBackend.start` passes verbatim into `tmux new-session`. `AuthConfig.agents: AgentsConfig = Field(default_factory=...)` with same malformed-block-tolerance + structlog-warn pattern as session/tunnel/notifications loader blocks. NEW `Settings.get_agent_command(agent_type) -> str` resolves agent → shell-string; unknown/None falls back to claude; for claude specifically, when `agents.claude_command` still equals the model default, the existing `CLAUDE_CLI_PATH` env-fallback (via `get_claude_cli_path()`) is preserved. **models.py**: `Session`, `SessionInfo`, `AttachableSession`, `CreateSessionRequest` all gained `agent_type: Optional[str] = None` (None default = legacy compat for pre-Phase-6 metadata + adopt-rows that won't be fingerprinted until Phase 7). **session_manager.py**: `create_session()` accepts `agent_type` kwarg; resolves precedence explicit-arg → project-default → "claude"; launches via `settings.get_agent_command(resolved_agent_type)` instead of the hardcoded `claude_cli` string; persists `agent_type` on the new `Session` instance (round-trips through `model_dump()` → `_write_metadata_atomic()` automatically). `get_session_info()` mirrors `agent_type` to the top-level `SessionInfo.agent_type`. `lifespan_startup()` got a one-shot backfill at the very top: if `self.session.agent_type is None` AND id is NOT prefixed `adopted:`, set to "claude" + persist + log `session_metadata_agent_type_backfilled count=N` (matches existing `owned_tmux_sessions` legacy-backfill cadence). Adopted sessions left None — Phase 7 fingerprint detector will fill them. **routes.py**: `POST /api/v1/sessions` now passes `body.agent_type` through to `create_session()` and includes it in the structured log. `GET /sessions` and `/sessions/attachable` automatically include `agent_type` via the model definitions (no route changes beyond the model updates). **config.example.json**: new `agents` block added between `notifications` and `common_slash_commands`; NeuralMesh3000 example project now has `"agent_type": "hermes"` to demonstrate per-project override. **Verification**: parse-check OK; server restart clean (port 8000 freed → respawned in 3s, log shows clean lifespan with no errors/tracebacks; backfill line did NOT appear because no existing session_metadata.json — expected backward-compat path); `/health` → 200 `{"status":"healthy"...}`; `/api/v1/sessions/attachable` → 401 (auth gate works → route exists); pydantic round-trip Session(agent_type='codex') → model_dump → reload preserves the field; legacy Session payload (no agent_type field) loads with `agent_type=None`; live config resolver returns the correct string for all 4 agent types + falls back to claude for unknown/None; existing `config.json` (no `agents` block) loaded fine — defaults kick in; existing `projects` (no `agent_type` field) loaded fine — default to "claude". **Files modified**: `src/config.py`, `src/models.py`, `src/core/session_manager.py`, `src/api/routes.py`, `config.example.json`. **Files NOT modified that someone might expect to be (sets next-phase expectations)**: `src/core/tmux_backend.py` (no fingerprint detector yet — Phase 7), `src/core/session_backend.py` (no `list_attachable_sessions` change needed since the dict→AttachableSession coercion just leaves agent_type=None), `src/main.py` (no lifespan signature change), `setup_auth.py` (no setup-flow change for agents block — defaults are usable as-is, operator can edit config.json post-setup). **Suggested validator-agent check (mostly backend; hard to validate via UI alone)**: (1) Endpoint `GET /api/v1/sessions` (auth'd) — response payload should include both `session.agent_type` and top-level `agent_type` fields. (2) Endpoint `POST /api/v1/sessions` with body `{"agent_type": "codex"}` — session should be created using the configured `codex_command` (won't fully launch unless `codex` CLI is on PATH for the Cloude server's user — verify by `tmux -L cloude list-sessions` then `capture-pane` on the new session to see whether the configured command was invoked). (3) Backward-compat: temporarily move `agents` block out of `config.json`, restart, confirm clean boot + `/health` 200; restore.
[PHASE-7-FINGERPRINT] [2026-04-24]: completed — `src/core/agent_fingerprint.py` (78 lines) ships the spec-mandated pattern table verbatim (4 agents × 3 patterns each, openclaw→hermes→codex→claude priority). Pure `detect_agent_type(scrollback) -> Optional[str]` with two-pass strategy: first scan last 50 lines (steady-state prompts/status), then last 2000 lines (banner fallback) only if pass 1 returns None and total lines > 50. Empty-input + None-equivalent inputs return None gracefully. Wired into `src/core/session_manager.py` `adopt_external_session()` immediately after `backend.capture_scrollback()` (around line 1170): bytes decoded UTF-8 with `errors="replace"`, fingerprint result logged at INFO via `logger.info("agent_fingerprint_detected", session=name, agent_type=...)`, then passed into `Session(agent_type=detected_agent_type)` so the existing metadata-write path round-trips it. None remains a valid outcome — adopted sessions whose fingerprint can't be matched stay agent_type=None and will render as "Unknown" in Phase 8. `tests/test_agent_fingerprint.py` (75 lines, 8 tests, all pass): parametrized real-capture detection for all 4 agents reading from `/Users/Adam/Dropbox/llmScratch/{claude,codex,hermes,openclaw}.txt`, empty/None graceful-return, unrelated-content returns None, two-pass proof (banner at line 0 + 80 lines of noise → 50-line tail confirmed clean of "OpenClaw" yet detector still returns "openclaw" via 2000-line pass), false-positive resistance (generic ╭─╰─ box-drawing → None, confirms patterns aren't anchored to corners alone). Server restarted clean on port 8000, no errors in lifespan log. Files created: `src/core/agent_fingerprint.py`, `tests/test_agent_fingerprint.py`. Files modified: `src/core/session_manager.py`. Suggested validator-agent live-fire: `tmux -L cloude new -d -s test_hermes "hermes"; sleep 6` → POST /api/v1/sessions/adopt with `{"name":"test_hermes","confirm_detach":true}` (auth'd) → expect response `agent_type=="hermes"` → cleanup `tmux -L cloude kill-session -t test_hermes`.
[PHASE-8-BADGES] [2026-04-24]: completed — 4 builtin agent SVGs copied into `client/assets/agents/{claude,codex,hermes,openclaw}.svg` (sizes 424B/1687B/19786B/2096B); all 4 reachable at `/static/assets/agents/<id>.svg` returning 200 from the live server. NEW `client/js/agentIcons.js` (75 lines, under the 80-line cap) — `window.AgentIcons` exposes synchronous `get(agentType)`, `preload()`, and the `KNOWN` whitelist. Sync getter is race-tolerant: if the SVG isn't cached when `get()` is called it returns a tiny letter-only placeholder (currentColor-inherited) and triggers an idempotent fetch; the next render tick (5s poller or sig change) reads from cache. Module fires `preload()` on load. Unknown/null agent_type renders an inline question-mark-in-circle SVG. Trust-boundary comment in the module header documents that builtins are inlined unsanitized and any FUTURE user-uploaded SVGs MUST be sanitized. `client/js/launchpad.js` got `_renderAgentBadge(agentType)` helper just above `renderRunningSessions`; the per-row `.running-session-badges` container now leads with the agent badge before RUNNING/EXTERNAL — design choice: agent identity is a stronger scan signal than the implied "RUNNING" status. Label abbreviation: openclaw → "CLAW" for visual parity (5/5/6/4 chars across CLAUDE/CODEX/HERMES/CLAW) per spec authorization. `_lastRunningSig` extended with `agent` so a backend-side agent_type change (e.g. Phase 7 fingerprint backfill) triggers a re-render. New-session modal (`showProjectNameModal`) extended with an `agent` <select> dropdown (Claude/Codex/Hermes/OpenClaw, defaults to `defaultAgentType` option, falls back to "claude"); `agent_type` propagates through both Enter-key and click confirm paths. Plumbed end-to-end: `createNewSession` → `API.createSession({agent_type})` + `API.createProject({agent_type})`; `openProjectFromFolder` → `saveProjectWithUniqueName({agent_type})` → `API.createProject({agent_type})` + `selectProject({agent_type})`; `selectProject` → `API.createSession({agent_type: project.agent_type \|\| 'claude'})`. `client/css/styles.css`: `.agent-badge` block added right after the existing `.badge-external` rule — mirrors `.badge` sizing (font-size 0.7em, padding 2px 6px, radius var(--radius-sm), letter-spacing 0.5px) for visual cohesion; uses `display: inline-flex` with 4px gap between icon and label; theme-tolerant `rgba(0,0,0,0.25)` background works on both dark and light themes; per-agent variant classes `agent-badge--{claude,codex,hermes,openclaw,unknown}` set color + border-color via existing `--badge-color-*` vars (Phase 1 already shipped these in `:root`); unknown variant uses `--color-fg-muted` + `--color-border` for the neutral gray. `client/index.html`: agentIcons.js script tag inserted between auth.js and launchpad.js (with comment noting load-order dependency). Verification: SVG fetch — 4× 200 from `192.168.1.250:8000` and `localhost:8000`; `node --check` clean on both `agentIcons.js` and `launchpad.js`. Files created: `client/assets/agents/{claude,codex,hermes,openclaw}.svg`, `client/js/agentIcons.js`. Files modified: `client/js/launchpad.js`, `client/css/styles.css`, `client/index.html`. **Suggested validator-agent check:** URL = https://192.168.1.250:8000; Steps: (1) load + auth, (2) confirm each running-session row has an agent-badge sitting LEFT of RUNNING — legacy adopted sessions render UNKNOWN gray, post-Phase-6 sessions show their colored badge with brand SVG, (3) click "create new project" → modal includes `agent` <select> with 4 options defaulting to Claude, (4) select Codex + create + verify the new row's badge is green CODEX. Expected: no broken images, no console errors, badge color matches `--badge-color-*` vars under all 5 themes.

[PHASE-4-5-TERMINAL] [2026-04-24]: completed — Phases 4+5 wired. `registry.js`: `applySession(agentType)` now real — null/unknown → falls through to `clearSession()`; matched manifest sets `#terminal-screen[data-session-theme=<id>]` AND applies the manifest's full `cssVars` map INLINE on the element via `style.setProperty()` (inline cascade beats `:root[data-theme]` without specificity wars or `!important`); fires `xtermThemeChange` listeners with the manifest's xterm palette. Tracks `sessionAppliedVarNames: Set` so `clearSession()` can deterministically `removeProperty()` only the keys it set (no `cssText=''` clobber of legitimate inline styles). Replay-gate: new `setReplayInProgress(flag)` exposed on `window.Themes`; while true, `applySession()` enqueues onto `deferredSessionQueue`; on falling edge we drain only the LAST enqueued agentType (coalesce to avoid mid-replay flicker). `terminal.js`: extracted `DEFAULT_XTERM_THEME` module const (lock-step with Claude fallback + styles.css :root); `XTerminal` constructor now reads `Themes.getActiveGlobal()?.xterm ?? DEFAULT_XTERM_THEME`; subscribed to `Themes.onXtermThemeChange()` post-`term.open()` — assigns `term.options.theme = newXtermTheme` (xterm + WebglAddon's `optionsChanged` listener re-uploads the glyph atlas automatically since 2021, no `term.refresh()` preempt needed per spec YAGNI). WebGL hardening: `WebglAddon` load wrapped + `addon.onContextLoss(() => addon.dispose())` registered; iOS Safari memory-pressure context loss disposes the addon and xterm transparently falls back to its built-in DOM renderer instead of going black. Recovery comment notes we deliberately don't auto-reload a fresh WebGL context (system pressure caused it; reload is the user-initiated clean path). `app.js`: `applySession(session.agent_type)` called in BOTH `showTerminal()` (line 244) and `returnToExistingTerminal()` (line 312); `clearSession()` called in both `showLaunchpad()` (line 207) and `showAuth()` (line 191) — covers session-create, session-return, session-destroy, manual launchpad navigation, logout, auth-required all four leave-paths. Theme.css scoping audit: codex/hermes/claw/matrix all already correctly scoped under `:root[data-theme="<id>"]` (global) and `#terminal-screen[data-session-theme="<id>"]` (terminal) — PASS, no fixes needed. `node --check` clean on all 3 modified JS files. Files modified: `client/js/themes/registry.js`, `client/js/terminal.js`, `client/js/app.js`. **Suggested validator-agent check:** URL = https://192.168.1.250:8000 (or current dev URL); Steps: (1) load + auth, (2) console: `Themes.applyGlobal('matrix')` → falling katakana visible behind launchpad cards + xterm bg goes pure black with green text on next session entry, (3) console: `Themes.applySession('hermes')` → `#terminal-screen` gets `data-session-theme="hermes"` + inline cssVars + xterm flips amber on black even though global is matrix, (4) console: `Themes.clearSession()` → attribute + inline vars gone, xterm reverts to matrix palette, (5) toggle prefers-reduced-motion in DevTools and reload Matrix → katakana canvas does NOT mount, static green wash from theme.css instead. Expected: every step produces visible/observable change without console errors; no FOUC; xterm never goes black on theme swap.
[BADGE-RIPOUT] [2026-04-24]: completed — Phase-8 frontend agent-badge UI surgically excised. Deleted `client/js/agentIcons.js` + `client/assets/agents/{claude,codex,hermes,openclaw}.svg`. Modified `client/index.html` (script tag + load-order comment removed), `client/js/launchpad.js` (1833→1783 LOC: dropped `_renderAgentBadge`, badge HTML in `renderRunningSessions`, `agent` field in `_lastRunningSig` change-detection, `agent` <select> + `defaultAgentType` param in `showProjectNameModal`, `agent_type` plumbing through `createNewSession`/`openProjectFromFolder`/`saveProjectWithUniqueName`/`selectProject`/`createProject`/`createSession` calls), `client/css/styles.css` (1690→1644 LOC: removed `.agent-badge` block + 5 variants + Phase-8 comment header). Backend `agent_type` infrastructure untouched (config.py AgentsConfig + ProjectConfig.agent_type, models.py Session/SessionInfo/AttachableSession.agent_type, session_manager.create_session/lifespan-backfill/adopt_external_session→detect_agent_type, agent_fingerprint.py + tests, routes.py POST /sessions still accepts agent_type — backend uses project default or "claude" when client omits it). Theme system's `app.js` reads of `session.agent_type` for `Themes.applySession()` left intact (not badge UI — drives per-session theme). `--badge-color-{claude,codex,hermes,openclaw}` :root vars left in place per spec (zero cost, future re-use). Verified: `grep -rn "AgentIcons|agentIcons|agent-badge|/static/assets/agents/|_renderAgentBadge" client/` → 0 hits. `node --check` clean on `launchpad.js` + `themes/registry.js`. `index.html` parses via `html.parser`. Backend tests untouched, no `src/` changes. Suggested validator-agent: hard-refresh `https://192.168.1.250:8000`, confirm running-session rows show only RUNNING + EXTERNAL/TMUX badges (no agent badge), new-session modal has no agent dropdown, console clean of any `AgentIcons`/`agent-badge` errors, Network tab shows no requests to `/static/assets/agents/*` or `/static/js/agentIcons.js`.

[PHASE-9-FINISHING] [2026-04-24]: completed — Pluggability surface wired end-to-end. **src/main.py**: `NoCacheStaticFiles._NO_CACHE_SUFFIXES` extended to `(.js, .html, .json)` so iOS Safari stops cross-session-caching `theme.json` (verified `cache-control: no-cache, must-revalidate` on `/static/css/themes/claude/theme.json` AND `/themes/test/theme.json`); new `_resolve_user_themes_dir()` honors `CLOUDE_USER_THEMES_DIR` env var, defaults to `~/Library/Application Support/cloude-code-menubar/themes/` (OS-portable via `Path.home()`), `app.mount("/themes", NoCacheStaticFiles(directory=..., html=False))` runs only when dir exists (graceful degradation logged as `user_themes_mount_skipped`); UNAUTH on purpose with comment block citing spec section "Architecture F" + T3 critique decision (theme assets contain no secrets — same threat model as `/static/*`). **src/api/routes.py**: `list_themes` extended with cross-root id-collision dedup — bundled wins, user theme silently dropped + structlog `theme_user_shadowed_by_builtin` warning. Within-root dedup, try-parse-or-skip, id-vs-dirname enforcement all preserved from Phase 2. **macOS/bootstrap.js**: NEW `provisionUserThemesDir()` helper (idempotent; creates dir + drops 1692-byte plain-markdown README ONCE; preserves user-edited README across re-runs — confirmed via 3-call test); wired into `bootstrapIfNeeded` as section `1a`, AFTER python detection and BEFORE asset resync so failures don't short-circuit it; helper exported from `module.exports` for testability. README documents schema (id/name/cssVars/xterm/effects), gives complete tiny example, calls out unauth contract + bundled-wins-on-collision rule. Lives OUTSIDE the resync allowlist so DMG upgrades will never touch it. **client/js/themes/registry.js**: net-new effects.js loader gated by 3-state localStorage allowlist `cloude.themeJsAllowlist` (`true` → load, `false` → skip silently, missing → prompt). Theme-styled modal built from existing `.modal-overlay/.modal-content/.modal-btn-primary/.modal-btn-secondary` classes (matches `app.js#showConfirmModal` pattern so it inherits whatever theme is currently active via `var(--color-bg)/--color-accent` etc.). 3 buttons: `[Never] [Allow once] [Always allow]` — focus-defaults to "Allow once" (safest non-persisting option). HTML escapes manifest.name. Coalesces concurrent prompts for the same themeId via `pendingConsentForTheme` + `consentResolveQueue` so double `applyGlobal` doesn't stack two modals. URL resolution: bundled → `/static/css/themes/<id>/<file>`, user → `/themes/<id>/<file>` — uses `manifest.source` to pick. Defensive path-traversal rejection on the effects filename. Same-origin script tag inherits `script-src 'self'` CSP — no nonce needed. Loaded-script registry prevents double-execution on re-applyGlobal. Bundled themes (Matrix) STILL go through the gate per spec belt-and-suspenders. Hooked into `applyGlobal()` fire-and-forget so CSS swap stays sync. Doc comment near loader cites spec section "Context" / Architecture F for the security reasoning. **Files modified**: `src/main.py` (+57/-3), `src/api/routes.py` (+18/-2), `macOS/bootstrap.js` (+98/-0), `client/js/themes/registry.js` (+178/-0). **Verification**: server clean restart (pid 98799), `Application startup complete.`, no errors/tracebacks; bundled `.json` cache header correct; user theme `.json` cache header correct (200 + `no-cache, must-revalidate`); `/api/v1/themes` → 401 (auth gate); direct call to `list_themes()` with collision-test fixtures returned `[claude:builtin, claw:builtin, codex:builtin, hermes:builtin, matrix:builtin, test:user]` with claude_count=1 + warning fired for the dropped user-claude; bootstrap helper test confirms create-on-missing + idempotent-when-present + preserve-user-edit; test fixtures cleaned up. **Suggested validator-agent check**: URL `http://192.168.1.250:8000/`, login, switch to Matrix theme via the selector. Expected: consent modal appears with copy "Theme &ldquo;Matrix&rdquo; ships a JavaScript module (effects.js)" and 3 buttons styled in current theme colors. Click "Always allow" → modal closes, falling katakana renders, localStorage `cloude.themeJsAllowlist` shows `{"matrix":true}`. Switch away then back to Matrix → no second prompt, FX runs immediately. Manually set `localStorage.setItem("cloude.themeJsAllowlist", JSON.stringify({"matrix":false}))` then reload + apply Matrix → no prompt, no canvas, just CSS theme.
[TERMINAL-THEME] [2026-04-24]: completed — New "terminal" theme shipped at `client/css/themes/terminal/{theme.json,theme.css}`. Manifest: id=terminal, 70 cssVars (all radius=0, all glow/shadow=transparent), 19-key xterm canonical palette per spec (background #000000, fg #C7C7C7, ANSI 16-color VT100 with #CD0000/#00CD00/#CDCD00/#0000EE/#CD00CD/#00CDCD/#E5E5E5 + bright variants #FF0000/#00FF00/etc). theme.css (7.4KB): scoped to `:root[data-theme="terminal"]`, global wildcard `*, *::before, *::after { border-radius: 0 !important; box-shadow: none !important; text-shadow: none !important; backdrop-filter: none !important }` + monospace force + flat form controls + sharp focus rings + inverse-video selection + minimal scrollbar. NO effects.js per spec (static theme). Verified: JSON parses (70 vars + 19 xterm), CSS braces 26/26 match. Skill: sme (GLYPH persona — xterm canonical/VT100/web-theming composite). [MATRIX-DEBUG] [2026-04-24]: root-caused why Matrix theme animation never rendered. Two compounding bugs in `client/js/themes/registry.js#injectEffectsScript()`: (A) effects.js authored as ES module (`export function init()`) but loaded via classic `<script src=>` tag — browsers SyntaxError on `export` at parse time and the script silently dies; (B) even if it loaded, registry only injected the tag and never CALLED `init()` — bundled effects.js has zero top-level side effects, only exports. Switched loader to dynamic `import(url)` so it parses as a module AND yields the namespace, then explicitly invokes `mod.init({themeContext})` (with `mod.default.init` fallback). Also added `manifest.source === 'builtin'` short-circuit in `maybeLoadEffects()` per DAR — bundled themes ship with the app and don't need a consent prompt (consent stays for `/themes/*` user-authored themes). Verified: `node --check registry.js` pass; dev server serves the patched bytes (HTTP 200, both `import(url)` and `manifest.source === 'builtin'` strings present). User retest: open DevTools console → `delete localStorage["cloude.themeJsAllowlist"]` (clears any stale "Never" entry from earlier prompt) → hard-refresh → switch theme to Matrix → falling katakana canvas should appear behind launchpad cards immediately, no consent modal. Files modified: `client/js/themes/registry.js`.

[MODAL-RIPOUT] [2026-04-24]: completed — Removed switch-session confirm modal across all 3 entry points in `client/js/launchpad.js`: `_handleAttachRunningSession` (running-session row click), `createNewProject` "already running" branch, `selectProject` "already running" branch. Promoted each modal "yes" callback (adoptSession w/ confirm_detach=true / detachAndCreateNew / detachAndOpenProject) to direct execution — no prompt, swap is instant. Pruned orphan helper `_getCurrentSessionLabel` (was only used by the 3 removed copy-strings). Preserved unrelated confirms: destroy-session (line 413), delete-project (~630), reset-server (~830), logout (app.js:364). Shared primitive `showConfirmModal` left intact (still used by 4 preserved flows). Verified: zero matches for `switch session?|will detach` + zero `_getCurrentSessionLabel` refs; `node --check launchpad.js` clean.

[PHASE-10-TESTS] [2026-04-24]: completed — Two new test modules shipped, full suite 240/240 green in 7.17s. **tests/test_themes_endpoint.py** (14 tests, 0.41s): manifest discovery (valid / missing-required-field / malformed-json / id-vs-dirname mismatch — all skip-and-warn), bundled vs user `source` labeling, bundled-wins on cross-root id collision, sort order (bundled alpha-by-name then user alpha-by-id), `401` without auth, pathological dirs (empty / no theme.json / binary garbage / hidden dotdirs), client-supplied `source` field overwritten server-side. Uses minimal FastAPI app with `dependency_overrides[require_auth]` for the discovery tests + a separate fixture without override for the 401 test; monkeypatches `routes_mod._bundled_themes_root` / `_user_themes_root` to `tmp_path` subdirs (request-time helpers, no app-build-time scanning); patches `routes_mod.logger` to a `MagicMock` to inspect warning event-names without touching structlog plumbing. **tests/test_session_agent_type.py** (41 tests, 0.27s): pydantic round-trip on Session/SessionInfo/AttachableSession across all 4 agent types + None; `AgentsConfig` defaults match the corrected CLI invocations verbatim; `Settings.get_agent_command()` matrix (4 known + case-insensitive + unknown/None/empty fallback + auth-config-load-failure tolerance + `CLAUDE_CLI_PATH` override applies-when-default + ignored-when-customized + bare-claude-no-rewrite); `ProjectConfig` Literal validation accepts 4 known + rejects unknown + defaults to claude; `CreateSessionRequest` accepts explicit/None/missing + arbitrary string (forward-compat); legacy session JSON without `agent_type` field deserializes cleanly; pure `backfill_agent_type()` function (owned→claude, adopted→None, idempotent on second call, returns 0 for None session, owned-set-None falls back to id-prefix heuristic, leaves all 4 known agent types alone). **Production code changes (3, all in service of test hardening)**: (1) `src/core/session_manager.py` — extracted top-level `backfill_agent_type(session, owned_tmux_sessions) -> int` pure function from inline `lifespan_startup` block (preserves identical behavior; lifespan now calls the helper). (2) `src/api/routes.py::_load_manifest` — added `UnicodeDecodeError` to the parse-failure exception tuple; was a real bug — `binary` bytes in a `theme.json` raised `UnicodeDecodeError` (a `ValueError` subclass, NOT `OSError`) which escaped past the existing `(OSError, json.JSONDecodeError)` catch and would have 500'd the endpoint on hostile/corrupted input. Now logs `theme_manifest_parse_failed` + skips. (3) None — bundled-wins cross-root dedupe was already shipped in Phase 9. **Files created**: `tests/test_themes_endpoint.py`, `tests/test_session_agent_type.py`. **Files modified**: `src/core/session_manager.py`, `src/api/routes.py`. Suite-level: 240/240 pass (was 185 = 177 baseline + 8 Phase-7), no flakes, no test pollutes another (env bootstrap each file via `os.environ.setdefault`).
[CARD-MATCH] [2026-04-24]: Project cards (.project-item) now use --color-accent-bg-soft to match .running-session-row, harmonizing scrim against translucent themes (Matrix). Other themes unchanged in feel.
[MATRIX-DESTROY] [2026-04-24]: Fixed leak where Matrix katakana canvas + RAF persisted after switching to another theme. Root cause: registry.js tracked `loadedEffectsScripts` (themeIds) but never held a reference to the loaded module, so destroy() had nothing to call. Added `activeEffectsModule`/`activeEffectsThemeId` module state; applyGlobal() now invokes destroy() on the outgoing module before swapping (skipped on same-theme re-apply); injectEffectsScript() registers the module post-init AND race-guards by destroying immediately if the user swapped away while import() was in flight. effects.js destroy() was already idempotent (canvas null-check, rafId guard, handler null-guards) — no changes needed there. Also drops the loadedEffectsScripts entry on destroy so future re-apply re-fetches cleanly. registry.js 628→677 LOC. node --check pass on registry.js + module-load pass on effects.js. Files modified: client/js/themes/registry.js.
[THEME-POKEMON] [2026-04-24]: completed — Pokemon theme shipped at `client/css/themes/pokemon/theme.json`. Cobalt navy (#0B1E3F) base, Pikachu yellow (#FFCB05) accent, Pokemon navy (#3B4CCA) strong-accent, Pokeball red (#EE1515) danger. 70 cssVars + 19-key xterm palette. JSON parses; bundled fetch returns 200 on both 127.0.0.1:5001 and 192.168.1.250:5001. No theme.css, no effects.js — flat-vars only per spec.
[THEME-BR] [2026-04-24]: Blade Runner shipped
[THEME-DRACULA] [2026-04-24]: Canonical Dracula shipped at `client/css/themes/dracula/theme.json`. Palette by Zeno Rocha (draculatheme.com): bg `#282a36`, fg `#f8f8f2`, accent `#bd93f9` (purple), strong `#ff79c6` (pink), success `#50fa7b`, warning `#f1fa8c`, danger `#ff5555`, info `#8be9fd`. Full 19-key xterm palette + 64 cssVars (derivative ramps generated from base hex). NO theme.css, NO effects.js — flat per canonical spec. JSON parse OK, server fetch HTTP 200.
[THEME-CALMING] [2026-04-24]: Calming theme shipped — light calm-tech (Amber Case principles + Japanese minimalism). `client/css/themes/calming/theme.json` (64 cssVars + 19-key xterm) with sage `#7ba374` accent, oat-cream `#F5F1E8` bg, mist-blue info, dusty-rose danger, dusty-gold warning, muted near-charcoal `#3a3a36` fg (no pure black). `client/css/themes/calming/theme.css` adds slow 30s `@keyframes calmingBreath` body-bg cycle (peaks at cooler `#F0EBDF`), softened light-mode shadows, terminal-scope wiring, and `prefers-reduced-motion: reduce` kills the animation. No effects.js — pure CSS breath. Verified: JSON parse OK (64 vars), CSS braces balanced 10/10, server returns 200 for both files at `192.168.1.250:5001/css/themes/calming/theme.{json,css}`.
[THEME-CANNABIS] [2026-04-24]: Cannabis theme shipped at `client/css/themes/cannabis/theme.json` — modern luxury dispensary aesthetic (Cookies/Houseplant/Beboe), explicitly anti-rasta. Deep forest `#1a2418` bg, hemp cream `#E8DEC4` fg, brushed brass `#C49C50` accent, sage `#7BAA5E` success, clay red `#C04848` danger, mossy `#88AA88` info. 70 cssVars + 19-key xterm palette (deep forest base, brass cursor, sage/moss greens, brass yellows, warm hemp whites, no pure rasta primaries). Tightened radii (sm:2/md:3/lg:6) for apothecary feel; deeper modal shadow. No theme.css, no effects.js per spec — flat luxe vars only. JSON parse OK (70 vars + 19 xterm keys); registry auto-discovers via `/api/v1/themes` directory scan.
[THEME-CORPORATE-V2] [2026-04-24]: Corporate Modern v2.0 shipped — Linear/Vercel/Stripe 2026 dark. `client/css/themes/corporate_v2/theme.json` (15 cssVars + 19-key xterm): cool near-black `#0A0A0B` bg, indigo `#6366F1` accent on `#06060a` header, fg `#fafafa`/muted `#a1a1aa`/subtle `#52525b`, hairline borders `#27272a`/`#1c1c1f`, semantic states (success `#10b981`, warning `#f59e0b`, danger `#ef4444`). Xterm: indigo cursor `#6366F1`, modern dark ANSI w/ Tailwind v4 brights. `client/css/themes/corporate_v2/theme.css`: 150ms ease-out hover/active on buttons (bg + border only, no `all`), 1px indigo focus ring (3px halo) on buttons, 1px indigo focus border on inputs/textarea/select, subtle 6%→2%→0% indigo gradient wash on header. NO effects.js. Verified: JSON parse OK (15 vars, 19 xterm keys), CSS braces balanced 9/9, server not running so live fetch N/A — registry auto-discovers on next boot.
[THEME-JAGER] [2026-04-24]: done — Jägermeister shipped at `client/css/themes/jagermeister/{theme.json,theme.css}`. Forest-bottle base `#0E2A1A` (shadow-inside-bottle) + `#1A5632` Pantone 357C elevated, parchment cream `#E8DCC4` fg, heritage orange `#F18A00` accent used sparingly (low-alpha bg/border ramps). 70 cssVars + 19-key xterm palette (orange routed to yellow + brightYellow ANSI slots, parchment to white, deep red `#C92D2D` reserved for danger only — no orange/red bleed). theme.css adds 1px ornate seam on `.header` (orange hairline top + forest-subtle bottom + 1px inset orange glow), monospace tightening (`letter-spacing: -0.01em`, ligatures off) for Albertus/Fraktur cadence, accent-text weight bump for engraved feel. NO animation, NO effects.js — gravitas over flair. JSON parse OK (70 cssVars, 19 xterm keys); asset fetch `/themes/jagermeister/theme.json` + `theme.css` both HTTP 200 on 127.0.0.1:5001.
[THEME-METAL] [2026-04-24]: Metal theme shipped at `client/css/themes/metal/theme.json` — thrash/heavy-classic discipline (Master of Puppets / Reign in Blood / Vulgar Display). Near-black `#0a0808` (2% red bias, no pure #000), oxblood crimson `#C4202A` accent, deeper `#A01818` strong, warm-bone `#E8E2D6` fg, chrome `#cccccc` info, muted thrash green `#7a9966` success, brass `#C49830` warning, blood-red `#FF2A2A` danger. 70 cssVars (parity with claude reference) + 19-key xterm palette (oxblood reds, chrome silvers, muted greens/blues — zero neons, zero glows). NO theme.css, NO effects.js — flat thrash palette IS the aesthetic. Verified: JSON parses, var-name parity with claude.json = 100% (no missing/extra), all xterm hex valid 6-digit, cursor set to oxblood `#C4202A` so the blink draws blood. Local cloudecode dev server not running (port 5001 owned by an n8n instance returning SPA fallback) so HTTP smoke deferred — file lives in correct bundled-themes dir alongside 11 peer themes and registry will auto-discover via `GET /api/v1/themes` on next boot.
[THEME-SNES] [2026-04-24]: done — SNES theme shipped at `client/css/themes/snes/theme.json` (manifest only, no theme.css, no effects.js per spec). SNES casing-grey `#3A3A40` bg + iconic `#6A3CC4` ABXY purple borders/accents, `#8A5CD4` cursor + accent-strong, 16-bit sprite ANSI palette (Mario red `#E83838`, 1-up green `#5CD065`, coin gold `#FFD030`, sky blue `#5BB5E8`, purple magenta) over a near-black `#1a1a20` ANSI black. Tightened radii (sm:2/md:3/lg:4) for plastic-cartridge feel; on-accent fg switched to bright `#E8E8F0` for contrast against purple. 70 cssVars + 19 xterm keys — full shape parity with claude reference manifest (no missing keys). JSON parse OK; loader is static-file mount at `/static/css/themes/snes/theme.json` — no registry edit needed (auto-discovered).
[THEME-LEGACY-APPLE] [2026-04-24]: Mac OS Classic / Platinum shipped at `client/css/themes/legacy_apple/theme.json`. LIGHT theme — cream `#DCD0BD` bg, Platinum `#EAEAEA` chrome, black fg, selection blue `#3D5BAA` accent. Rainbow Apple stripes mapped into ANSI brights: green `#5CAB58`, orange `#F0A028`, red `#D03028`, purple `#9E4F9E`, yellow `#F5BD2D`, blue `#3D5BAA`. Monaco font stack, period-correct flat radii (0px). NO theme.css, NO effects.js per spec. JSON parse OK; HTTP 200 on :5001.
[THEME-ACID-TRIP] [2026-04-24]: done — Acid Trip shipped at `client/css/themes/acid_trip/{theme.json,theme.css}`. Wilson/Moscoso x psy-trance: deep eggplant `#1A0838` ground, electric yellow `#FFEE00` fg, hot pink `#FF00C8` border, cyan `#00FFE0` accent (true wheel-complement of magenta border = Moscoso vibrating-edge clash). 64 cssVars + 19-key xterm (saturated complementary palette: red `#FF0040`, green `#00FF40`, blue `#7A00FF`, magenta `#FF00C8`, yellow `#FFEE00`, cyan `#00FFE0`). theme.css: `@keyframes acidHueShift` 60s linear infinite hue-rotate(0→360deg) on body = one slow LSD breath through the full color wheel; secondary 8s `acidAccentBreath` box-shadow pulse on `.btn-primary`/focus-visible; equiluminant inset border + cyan outer glow on `.card/.modal/.panel`. A11y: `prefers-reduced-motion: reduce` kills both animations. NO effects.js — pure CSS. Verified: JSON parse OK (64 cssVars + 19 xterm keys), CSS braces balanced 11/11; loader auto-discovers via `_bundled_themes_root()` scan in `src/api/routes.py:580` (no registry edit needed); server not running so live fetch deferred to next boot.
[THEME-GAMEBOY] [2026-04-24]: Game Boy DMG-01 shipped at `client/css/themes/gameboy/theme.json` — strict 4-color hardware palette (`#9bbc0f` lightest, `#8bac0f` light, `#306230` dark, `#0f380f` darkest). 70 cssVars + 19-key xterm. ANSI mapping by hardware semantics: black/cursor/fg=darkest, ANSI darks=dark, ANSI brights=light, brightWhite/bg=lightest. No fifth color anywhere — verified `grep -oE '#[0-9a-fA-F]{6}' | sort -u | wc -l` → 4. Radii zeroed (pixel-square corners), shadow nulled, accent-soft rgbas all derived from darkest `#0f380f`. NO theme.css (DMG was static), NO effects.js. JSON parses; registry auto-discovers on next boot.
[THEME-LEGACY-WINDOWS] [2026-04-24]: done — Legacy Windows shipped at `client/css/themes/legacy_windows/{theme.json,theme.css}`. Win 95/98 system colors verbatim: BTNFACE `#C0C0C0` bg, `#D4D0C8` elevated, ACTIVECAPTION `#000080` navy header w/ white CAPTIONTEXT, BTNSHADOW `#808080` borders, IBM CGA cmd.exe 16-color xterm palette (`#800000`/`#008000`/`#808000` darks, `#FF0000`/`#00FF00`/`#FFFF00` brights, `#C0C0C0` fg on `#000000`). 15 cssVars + 19 xterm keys. theme.css: stacked 1px 3D RAISED bevels (white top+left + dark bottom+right) on buttons/cards/panels/modal-content, SUNKEN bevels on inputs/textarea/select, sharp 0px border-radius globally within scope (`!important`), pressed-state bevel inversion on `:active`, navy `.modal-header` w/ white text, classic underlined navy/purple links, ACTIVECAPTION text selection, transitions killed. All selectors scoped under `:root[data-theme="legacy_windows"]`. NO effects.js per spec. Verified: JSON parse OK (15 cssVars + 19 xterm), CSS braces balanced 16/16 (4941 bytes, 40 scope-prefixed selectors). Live HTTP probe N/A on Dev tree (port 5001 owned by node — different app); registry auto-discovers via `/api/v1/themes` dir scan on next boot. Skill: sme (Vince Marrone — Win9x system-color + 3D bevel + IBM CGA composite persona).
[ICONS-THEMEIFY] [2026-04-24]: Reset Server + footer bird now theme-aware — both SVGs converted to `currentColor` (`stroke` for the refresh icon at `client/js/launchpad.js:515-516`, `fill` for the bird at `:525`). Reset Server inherits via existing `.reset-server-btn { color: var(--color-accent) }` (no CSS change needed). Footer bird link `.launchpad-footer a` now sets `color: var(--color-fg-muted)` default + `color: var(--color-accent)` on hover (was opacity-only). Geometry/viewBox untouched. Verified: `node --check` pass, CSS brace balance 0, zero remaining `stroke="#`/`fill="#` hardcoded refs in launchpad.js. Skills: sme + design-engineer.
[FAB-RELOCATE] [2026-04-25]: + button moved inline with project heading, ghost-styled
[FAB-RESTORE] [2026-04-25]: + button restored inline with project heading. Root cause: ghost trigger was rendered but visually invisible — 1.5px border in low-contrast `--color-border` + `+` glyph in `--color-fg-muted` with transparent fill = effectively no pixels readable on most themes. Markup, render path, wiring, and serve all verified intact (curl confirms FAB markup + heading-row class shipped in served launchpad.js). Fix: bumped resting visibility — `border: 2px solid var(--color-fg-muted)` (always-readable token used by all body text) + `opacity: 0.85` resting / `1.0` on hover. Kept ghost discipline (no solid fill, no orange), kept fan-out animation, kept all hover/focus/open accent retint downstream. File modified: client/css/styles.css (.new-fab__trigger block).
[MATRIX-CARD-OPACITY] [2026-04-25]: running-session cards now scrimmed in matrix theme
[MATRIX-SELECTOR-FIX] [2026-04-25]: dead #launchpad → #launchpad-screen

[SESSION-IDENTITY-V2] [2026-04-26]: per-session pinned theme + header identity swap

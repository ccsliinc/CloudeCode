# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
env/
ENV/
.venv

# Environment Variables
.env
.env.local
config.json

# Authentication
totp-qr.png
# TOTP pairing sentinel (runtime state)
.totp_paired

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/
/tmp/
*.log.*

# Session Data
session_metadata.json
pinned_themes.json
unread_state.json
/claude-projects/

# Image Uploads (per-session bucket — must never be committed)
.cloude_uploads/

# Auth State (refresh-token store — must never be committed)
refresh_tokens.db

# Test Coverage
.coverage
htmlcov/
.pytest_cache/

# Claude
.prompt_history/
troubleshooting/

.superpowers/

# Runtime lock files (session-scoped, PID-stamped — never meant to be tracked)
.claude/scheduled_tasks.lock

# Build-only workspace for the vendored CodeMirror bundle (see
# client/vendor/codemirror/VERSION.md) — node_modules/lockfile are build
# inputs, not runtime code; only the built bundle under client/vendor/ ships.
scripts/codemirror-vendor/node_modules/
scripts/codemirror-vendor/package-lock.json
